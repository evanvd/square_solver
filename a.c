#include <stdio.h>
void fun(void)
{
    printf("kal\n");
}
int main(void)
{
    int num;
    num = 1;
/* определить переменную с именем num */
/* присвоить значение переменной num */
    printf("я простой ") ;
/* использовать функцию printf() */
    printf("компьютер.\n");
    fun();
    printf ("Моей любимой цифрой является %d, так как она первая. \n", num);
}
